/**
 * Credential Vault — Public API
 * Re-exports for credential management
 */

export {
  CredentialVault,
  type CredentialEntry,
  type EncryptedCredential,
  type VaultConfig,
  type ICredentialPersistence,
  type MaskedCredential,
  type VaultEvent,
} from './credential-vault.js';
