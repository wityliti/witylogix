{
  "name": "@witylogix/bench-core",
  "version": "0.0.1",
  "private": true,
  "description": "Provisioning engine and shared types for Witylogix Bench.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./config": "./src/config.ts",
    "./provider": "./src/provider.ts"
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo 'lint skipped — ESLint not installed (typecheck-only for now)'"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "tar": "^7.4.0",
    "yaml": "^2.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/tar": "^6.1.13",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^1.0.0"
  }
}
