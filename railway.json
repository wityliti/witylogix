{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install && pnpm turbo build --filter=@witylogix/api"
  },
  "deploy": {
    "startCommand": "pnpm --filter @witylogix/api start"
  }
}
